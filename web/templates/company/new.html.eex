<h2>New company</h2>

<%= render "form.html", changeset: @changeset,
                        action: company_path(@conn, :create),
                        terms: @terms, contacts: @contacts %>

<%= link "Back", to: company_path(@conn, :index) %>
