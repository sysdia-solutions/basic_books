<h2>Show contact</h2>

<ul>

  <li>
    <strong>Name:</strong>
    <%= @contact.name %>
  </li>

</ul>

<%= link "Edit", to: contact_path(@conn, :edit, @contact) %>
<%= link "Back", to: contact_path(@conn, :index) %>
